apiVersion: v1
kind: Service
metadata:
  name: colourserver-blue
  labels:
    bgp: public
spec:
  type: LoadBalancer
  selector:
    app: colourserver-blue
  ports:
  - port: 80
    targetPort: 80
  
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: colourserver-blue-deployment
  namespace: default
  labels:
    app: colourserver-blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: colourserver-blue
  template:
    metadata:
      labels:
        app: colourserver-blue
    spec:
      containers:
      - name: colourserver
        image: ghcr.io/cbellee/colourserver:latest
        resources:
          requests:
            cpu: 250m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        env:
          - name: COLOUR
            value: blue
