# Changes - 2026-07-09

## Summary
This note captures the major infrastructure and GitOps changes made today across Terraform, Flux, and platform bootstrapping.

## Committed And Pushed

### Flux + Terraform integration
- Added Terraform-native Flux bootstrap using the `fluxcd/flux` provider (`flux_bootstrap_git`) instead of local-exec script bootstrap.
- Added Flux-related Terraform inputs for repo URL, auth username, and token.
- Updated Terraform docs and examples for provider-based bootstrap.
- Added provider URL guard behavior for non-bootstrap runs to avoid destroy/apply validation failures when Flux bootstrap is disabled.

Related commits:
- `c107c3d` - Use Terraform Flux provider for cluster bootstrap
- `d4e3f38` - Guard Flux provider URL when bootstrap disabled

### Git hygiene for Terraform plans
- Added explicit ignore entry for plain `tfplan` files.
- Removed tracked `tf-k8s-cluster/tfplan` artifact from git index.

Related commits:
- `f8dab61` - Ignore and untrack Terraform tfplan artifact
- `d2ffdfc` - Ignore plain tfplan filenames

### Platform-first MikroTik DNS controller
- Added `mikrotik-dns-controller` under `gitops/platform/`.
- Wired it into `gitops/platform/kustomization.yaml` so it reconciles before apps (apps already depend on platform).
- Added namespace/RBAC/deployment manifests for the controller.
- Removed `imagePullSecrets` from controller deployment (public GHCR image path).

Related commit:
- `b67b1bc` - Add MikroTik DNS controller to Flux platform layer

## In Progress (Not Yet Committed)
These working-tree changes are currently present and validated:

- `gitops/clusters/kube-cluster-01/infrastructure.yaml`
  - Added Flux SOPS decryption settings:
    - `spec.decryption.provider: sops`
    - `spec.decryption.secretRef.name: sops-age`

- `gitops/platform/mikrotik-dns-controller/deployment.yaml`
  - Removed inline plaintext credentials secret from deployment file.

- `gitops/platform/mikrotik-dns-controller/kustomization.yaml`
  - Added `secret.yaml` to resources.

- `.sops.yaml`
  - Added SOPS creation rule for `gitops/platform/mikrotik-dns-controller/secret.yaml`.

- `gitops/platform/mikrotik-dns-controller/secret.yaml`
  - Added encrypted SOPS-managed Kubernetes Secret manifest (`mikrotik-credentials`).

## Cluster-side setup completed today
- Created/updated Flux decryption key secret in cluster:
  - `secret/sops-age` in `flux-system` namespace.

## Operational fixes completed today
- Resolved libvirt OVMF firmware startup error by providing the missing `OVMF_VARS_4M.fd` path expected by domain XML.

## Next Recommended Steps
1. Confirm encrypted secret contains final MikroTik credentials.
2. Commit and push the in-progress SOPS + decryption wiring files.
3. Reconcile Flux source/kustomizations and verify controller health:
   - `kubectl -n mikrotik-dns-controller get deploy,pods`
   - `kubectl -n mikrotik-dns-controller logs deploy/mikrotik-dns-controller`
4. Validate DNS sync behavior for annotated LoadBalancer services.
